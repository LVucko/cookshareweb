import image from "../misc/loading.gif";
const Loading = () => {
  return (
    <div className="tight-row">
      <img className="row" alt="loading" src={image}></img>
    </div>
  );
};

export default Loading;
